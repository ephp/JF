{% set value = pratica.value(question.question) %}
<div>
    <h4><abbr title="{{ question.question.example }}">{{ question.question.question }}</abbr></h4>
    <input type="number" value="{% if value %}{{ value.response }}{% endif %}" name="risposta[{{ question.question.id }}]" />
</div>