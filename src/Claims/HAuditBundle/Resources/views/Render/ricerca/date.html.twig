<div>
    <label>{{ question.domandaLocale(app.user.locale) }}</label>
    <input type="text" value="{% if ricerca[question.id] is defined %}{{ ricerca[question.id] }}{% endif %}" class="auto_date" name="ricerca[{{ question.id }}]" />
</div>