{% set titolo = 'Novo Countdown per '~gestore.nome %}

<div>
    {{ testo|nl2br }}
    <br/>
    <br/>
    <small>
        {{ gestore.nome }}
    </small>
</div>

{% if allegati|length > 0 %}
<hr/>
<div>
    Sono presenti {{ allegati|length }} allegati
</div>
{% endif %}
