{% set value = pratica.value(question.question) %}
<div>
    <h4><abbr title="{{ question.question.esempioLocale(app.user.locale) }}">{{ question.question.domandaLocale(app.user.locale) }}</abbr></h4>
    <input id="risposta_{{ question.question.id }}" type="text" value="{% if value %}{{ value.response }}{% endif %}" class="percentual check-save" min="0" max="100" name="risposta[{{ question.question.id }}]" />
</div>