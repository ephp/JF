{% extends '::base.html.twig' %}
{% form_theme edit_form '::form_aries_layout.html.twig' %}
{% block body -%}
<div class="page-header">
    <div class="icon">
        <span class="ico-circle-arrow-up"></span>
    </div>
    <h1>Priorità <small>Claims</small></h1>
</div>
{{ form_start(edit_form) }}
    <div class="row-fluid">
        <div class="span6">
            <div class="block">
                <div class="head">                                
                    <h2>Nuova</h2>
                </div>                                        
                <div class="data-fluid">
                    {{ form_row(edit_form.priorita) }}
                    {{ form_row(edit_form.onAssign) }}
                </div>
            </div>
        </div>
        <div class="span6">
            <div class="block">
                <div class="head">                                
                    <h2>&nbsp;</h2>
                </div>                                        
                <div class="data-fluid">
                    {{ form_row(edit_form.css) }}
                    {{ form_row(edit_form.show) }}
                </div>
            </div>
        </div>
    </div>
    {{ form_rest(edit_form) }}
{{ form_end(edit_form) }}
<a href="{{ path('eph_priorita') }}">
    <span class="label label-inverse">Torna indietro</span>
</a>
{% endblock %}
