{% set value = pratica.value(question.question, 0) %}
<div>
    <h4><abbr title="{{ question.question.example }}">{{ question.question.question }}</abbr></h4>    
    <textarea id="risposta_{{ question.question.id }}" class="expand autogrow" name="risposta[{{ question.question.id }}]">{% if value %}{{ value.response }}{% endif %}</textarea>
</div>