{% extends '::base.html.twig' %}
{% form_theme edit_form '::form_aries_layout.html.twig' %}

{% block body -%}
<div class="page-header">
    <div class="icon">
        <span class="ico-cog"></span>
    </div>
    <h1>Sistema <small>Modifica</small></h1>
</div>
{{ form_start(edit_form) }}
    <div class="row-fluid">
        <div class="span6">
            <div class="block">
                <div class="head">                                
                    <h2>Sistema</h2>
                </div>                                        
                <div class="data-fluid">
                {{ form_rest(edit_form) }}
                </div>                                        
            </div>
        </div>
    </div>
{{ form_end(edit_form) }}
<a href="{{ path('eph_claims_h_sistemi') }}">
    <span class="label label-inverse">Torna indietro</span>
</a>

{% endblock %}
