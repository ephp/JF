{% set value = pratica.value(question.question) %}
<div>
    <h4><abbr title="{{ question.question.example }}">{{ question.question.question }}</abbr></h4>
    <input id="auto_date" type="text" value="{% if value %}{{ value.response }}{% endif %}" class="auto_date" name="risposta[{{ question.question.id }}]" />
</div>
